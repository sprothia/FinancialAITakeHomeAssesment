import React from 'react';
import './Topbar.css';

const TopBar = () => {
    return (
        <div className="top-bar">
            <h1 className="top-bar-title">Financial AI Analyzer</h1>
        </div>
    );
}

export default TopBar;
